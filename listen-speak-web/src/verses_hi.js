// Hindi built-in verse sets.
// Translation: Hindi (bolls slug — see BOLLS_TRANSLATIONS.hi).
// Topic sets are generated + published to PartyKit by convert_topic_kjv_to_lang.mjs hi;
// this array can stay empty (loose/random verses still work via the live bolls.life fetch).
export const VERSE_SETS_HI = [];
