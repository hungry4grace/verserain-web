// Khmer (ភាសាខ្មែរ) built-in verse sets.
//
// Empty for now: no free, no-API-key Khmer Bible source exists among the
// services this app already uses (bolls.life, api.getbible.net,
// bible.helloao.org — verified 2026-09-12, none have real Khmer text).
// Real verse content is planned via Bible Brain (dbt.io) once an API key
// is obtained and the fetch path (App.jsx's fetchKhmerVerse, proxied
// through a server-side endpoint like api/dbt-passage.js) is wired up.
export const VERSE_SETS_KM = [];
